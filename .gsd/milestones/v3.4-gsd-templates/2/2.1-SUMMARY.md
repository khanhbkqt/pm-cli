# Plan 2.1 Summary

**Objective:** Wire GSD template loaders into `createMilestone` and `addPhase`.

**Changes:**
- `src/core/milestone.ts` — added `projectRoot?: string` param; after DB insert, calls `loadGsdTemplate` + `populateMilestoneTemplate` + `writeMilestoneContent`; silently skips if template not found.
- `src/core/phase.ts` — added `projectRoot?: string` param; after DB insert, calls `loadGsdTemplate` + `populatePhaseTemplate` + `writePhaseContent`; silently skips if template not found.

**Imports added:**
- `milestone.ts`: `loadGsdTemplate`, `populateMilestoneTemplate` from `./template_gsd.js`; `writeMilestoneContent` from `./content.js`
- `phase.ts`: `loadGsdTemplate`, `populatePhaseTemplate` from `./template_gsd.js`; `writePhaseContent` from `./content.js`

**Verification:**
- `npx tsc --noEmit` — ✅ zero errors
- All 44 unit tests in milestone/phase/plan test files pass

**Tests Added (milestone.test.ts):**
- `createMilestone with projectRoot writes MILESTONE.md from template when template exists` ✅
- `createMilestone with projectRoot silently skips if no template` ✅

**Tests Added (phase.test.ts):**
- `addPhase with projectRoot writes PHASE.md from template when template exists` ✅
- `addPhase with projectRoot silently skips if no template` ✅
