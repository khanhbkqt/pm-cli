# Plan 2.2 Summary

**Objective:** Extend `createPlan` to auto-populate from PLAN.md template when `projectRoot` is given but no explicit `content`.

**Changes:**
- `src/core/plan.ts` — added `loadGsdTemplate`, `populatePlanTemplate` imports; refactored filesystem-write block from `if (content && projectRoot)` to `if (projectRoot)` with inner branch:
  - `content !== undefined` → write explicit content as-is (existing behaviour preserved)
  - `content === undefined` → auto-load `PLAN.md` template, populate, write; silently skips if no template

**Verification:**
- `npx tsc --noEmit` — ✅ zero errors
- 44 tests pass across milestone/phase/plan test files

**Test Added (plan.test.ts):**
- `createPlan with projectRoot and no content auto-populates from PLAN.md template` ✅
  - Verifies substitutions: `{N}` → phaseNumber, `{M}` → planNumber, `{W}` → wave, `{Descriptive Name}` → name
  - Verifies no raw placeholders remain in written file
