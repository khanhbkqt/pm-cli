---
phase: 3
plan: 1
wave: 1
---

# Plan 3.1: CLI Passthrough & Tests

## Objective
Pass `projectRoot` through to `createMilestone` and `addPhase` in their CLI command handlers, and update CLI integration tests to verify template-populated markdown files are generated.

## Context
- .gsd/SPEC.md
- .gsd/ROADMAP.md
- src/cli/commands/milestone.ts
- src/cli/commands/phase.ts
- tests/milestone-cli.test.ts
- tests/phase-cli.test.ts
- src/core/identity.ts

## Tasks

<task type="auto">
  <name>Update CLI Handlers</name>
  <files>
    src/cli/commands/milestone.ts
    src/cli/commands/phase.ts
  </files>
  <action>
    - In `src/cli/commands/milestone.ts`, import `findProjectRoot` from `../../core/identity.js`.
    - In the `create` command action, call `findProjectRoot()` and pass `projectRoot` to `createMilestone`.
    - Update the `console.log` output on success to mention that the file was created (similar to the output in `plan.ts` CLI).
    - Repeat the exact same steps in `src/cli/commands/phase.ts` for the `add` command action and `addPhase` function.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Code compiles without errors and both cli handlers pass projectRoot to core functions.</done>
</task>

<task type="auto">
  <name>Update CLI Integration Tests</name>
  <files>
    tests/milestone-cli.test.ts
    tests/phase-cli.test.ts
  </files>
  <action>
    - In `tests/milestone-cli.test.ts`, locate the `pm milestone create creates a milestone` test. Since the tests use a tempDir as project root, add an assertion to verify `fs.existsSync(path.join(tempDir, '.pm/milestones/v1/MILESTONE.md'))` is true.
    - In `tests/phase-cli.test.ts`, locate the phase creation test and similarly assert `fs.existsSync(path.join(tempDir, '.pm/milestones/v1/1/PHASE.md'))` is true.
  </action>
  <verify>npx vitest run tests/milestone-cli.test.ts tests/phase-cli.test.ts</verify>
  <done>Both CLI test suites pass all tests with standard execution, validating filesystem changes.</done>
</task>

## Success Criteria
- [ ] `pm milestone create` automatically creates the milestone markdown file via CLI
- [ ] `pm phase add` automatically creates the phase markdown file via CLI
- [ ] `milestone-cli.test.ts` integration tests pass
- [ ] `phase-cli.test.ts` integration tests pass
